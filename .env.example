# Backend configuration
DB_PATH=./data/chipax_data.db
CORS_ORIGINS=http://localhost:3001
PORT=5555
# Optional: switch to Postgres when running docker-compose.override.yml
DATABASE_URL=postgresql+psycopg2://ofitec:ofitec@postgres:5432/ofitec

# Frontend configuration
NEXT_PUBLIC_API_BASE=http://localhost:5555/api
BACKEND_URL=http://localhost:5555

# Postgres service (docker-compose.override)
POSTGRES_DB=ofitec
POSTGRES_USER=ofitec
POSTGRES_PASSWORD=ofitec
